{
  "Servers": {
    "1": {
      "Name": "Deepiri PostgreSQL",
      "Group": "Servers",
      "Host": "postgres",
      "Port": 5432,
      "MaintenanceDB": "deepiri",
      "Username": "deepiri",
      "Password": "${POSTGRES_PASSWORD}",
      "SSLMode": "prefer",
      "Comment": "Deepiri Development PostgreSQL Database — password is a placeholder, provide via POSTGRES_PASSWORD at runtime"
    }
  }
}

