apiVersion: v1
kind: Secret
metadata:
  name: realtime-gateway-secret
  namespace: default
  annotations:
    description: "Secret for Realtime Gateway Service - confidential environment variables"
type: Opaque
stringData:
  # Add any confidential Realtime Gateway secrets here
  # Example:
  # WEBSOCKET_SECRET: "your-websocket-secret-here"

