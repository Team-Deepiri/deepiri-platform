apiVersion: v1
kind: Secret
metadata:
  name: task-orchestrator-secret
  namespace: default
  annotations:
    description: "Secret for Task Orchestrator Service - confidential environment variables"
type: Opaque
stringData:
  # Add any confidential Task Orchestrator secrets here
  # Example:
  # API_KEY: "your-api-key-here"

