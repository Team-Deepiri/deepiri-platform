apiVersion: v1
kind: Secret
metadata:
  name: frontend-dev-secret
  namespace: default
  annotations:
    description: "Secret for Frontend Dev Service - confidential environment variables"
type: Opaque
stringData:
  # Add any confidential Frontend secrets here
  # Example:
  # VITE_FIREBASE_API_KEY: "your-firebase-api-key"
  # VITE_ANALYTICS_KEY: "your-analytics-key"

