apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-secret
  namespace: default
  annotations:
    description: "Secret for API Gateway service - confidential environment variables"
type: Opaque
stringData:
  # Add any confidential API Gateway secrets here
  # Example:
  # JWT_SECRET: "your-jwt-secret-here"
  # API_KEY: "your-api-key-here"

