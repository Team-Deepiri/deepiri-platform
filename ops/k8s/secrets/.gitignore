# Kubernetes Secrets - NEVER commit actual secrets
# This directory is for k8s secret YAML files
# Only example/template files and per-service secret scaffolds should be committed

# Ignore the main shared secrets file (actual secrets)
secrets.yaml

# Ignore production/staging environment secrets
*-prod.yaml
*-production.yaml
*-staging.yaml

# Ignore any other secrets patterns
*.secrets.yaml
*.secrets.yml

# Allow example and template files
!*.example
!*.example.*
!*-template.*
!*.template

# Allow documentation and scaffolds
!README.md
!.gitkeep
!*-secret.yaml  # Per-service secret scaffolds/templates are OK to commit
