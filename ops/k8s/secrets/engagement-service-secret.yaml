apiVersion: v1
kind: Secret
metadata:
  name: engagement-service-secret
  namespace: default
  annotations:
    description: "Secret for Engagement Service - confidential environment variables"
type: Opaque
stringData:
  # Redis Password
  REDIS_PASSWORD: "redispassword"

