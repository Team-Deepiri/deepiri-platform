apiVersion: v1
kind: Secret
metadata:
  name: challenge-service-secret
  namespace: default
  annotations:
    description: "Secret for Challenge Service - confidential environment variables"
type: Opaque
stringData:
  # Add any confidential Challenge Service secrets here
  # Example:
  # API_KEY: "your-api-key-here"

