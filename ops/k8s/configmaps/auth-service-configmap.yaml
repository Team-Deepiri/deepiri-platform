apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: default
  annotations:
    description: "Cloud portal Auth Service — platform DB on postgres-platform"
    plane: cloud-portal
data:
  NODE_ENV: "production"
  PORT: "5001"
  GOOGLE_CLIENT_ID: "your-google-client-id.apps.googleusercontent.com"
