apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-config
  namespace: default
  annotations:
    description: "Cloud portal Registry — postgres-platform"
    plane: cloud-portal
data:
  NODE_ENV: "production"
  PORT: "5003"
