apiVersion: v1
kind: ConfigMap
metadata:
  name: messaging-service-config
  namespace: default
  annotations:
    description: "ConfigMap for Messaging service — chat, notifications, web push, webhooks"
data:
  NODE_ENV: "development"
  PORT: "5010"
  SYNAPSE_URL: "http://synapse:8002"
  REALTIME_GATEWAY_URL: "http://realtime-gateway:5008"
  CYREX_URL: "http://cyrex:8000"
