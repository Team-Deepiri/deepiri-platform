# Full local dev stack moved to deepiri-control-plane
#
# Clone: git clone git@github.com:Team-Deepiri/deepiri-control-plane.git
# Then:  bash setup-deepiri-dev.sh
#
# This cloud portal repo uses docker-compose.yml only (VPS prod).
name: deepiri-dev-moved-to-control-plane

services: {}
